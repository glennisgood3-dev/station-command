# station-command · 站 3 補票：自主循環相關（未發布）

依據：Spec v1.8（定稿）新增節 §3.5 第八欄／§4.6 待寫佇列／§5.3a session 內 loop／§5.3b＋§7.5（deferred-to-CI）
規則：Pocock to-tickets 原文 ＋ 本專案**八欄位**（§3.5，新增「不可逆動作」欄）
範圍：**只補新票，編號自 T-21 起**；既有 21 張的必要增修列於末節，不重寫原檔。
狀態：**已過使用者 quiz，本檔為定稿。** T-23 依裁示併入 T-09（票號不重排，T-24／25／26 維持原號）。

> **前提事實（Commander 以一級來源裁定，非轉述）**：以「**寫不了**」為準，本批照案執行。今日實測三次——MCP `push_files` → 403 `Resource not accessible by integration`；`get_file_contents` → 成功；Bash `gh api` → session proxy 攔截。
> ADR-NP-009 的「actor＝使用者本人」屬**推論**（假設寫得進去時的歸因結果），「寫不了」屬**量測**；兩者已於 ADR 與 Spec §4.6 分為 ①【量測】②【推論】兩層——**① 修好則 §4.6 廢止，② 仍須等 CI**。

---

## T-21 · 待寫佇列基礎設施（格式、產生權、套用、回驗、對帳出列）
**Blocked by**：T-07
**What it delivers**：手動階段的**唯一寫入路徑**——佇列檔格式、產生權不變式、由 dispatch 產出的本機套用腳本、套用後回驗、每輪 loop 開頭的對帳出列。完成即可獨立 demo：手工造一筆佇列項 → 跑腳本 → GitHub 上真的落地 → 回驗通過 → 該項自動出列。
- **REQ-ID**：REQ-§4.6
- **驗收條件（可執行）**：① 造三筆佇列項（label 集合／開關 issue／留言）⇒ 套用後以 issues API 直讀，三者狀態與 payload 相符；② **冪等**：同一佇列連續套用兩次 ⇒ 因套用前讀現況比對而全部跳過，無重複發票或重複寫入；③ **回驗**：人為讓一筆套用結果不符 ⇒ 該筆留在佇列並具名回報，不得標為已套用；④ **對帳出列**：使用者手動先落地其中一筆，再跑 loop 開頭對帳 ⇒ 該筆自動出列；⑤ **產生權不變式**：以 run 的身分嘗試產生 label 類佇列項 ⇒ 被拒並具名（label 類只能由 gate 產生、issue／milestone 建立只能由 intake 產生）；⑥ **遺失降級**：刪除佇列檔後重跑 ⇒ 具名回報「待寫佇列不存在，本批動作將重新產生」，GitHub 既有狀態不受影響。**紅燈設計**：先寫②與⑤兩條斷言 ⇒ 必紅（未實作時重複落地、且 run 產得出 label 類）。**獨立預期值來源**＝人造佇列項的 payload 原值與 Spec §4.6 的產生權表。
- **depends_on**：[T-07]
- **executor**：`fullstack-developer`｜**basis**：這張穿過「產生 → 落地 → 回驗 → 出列」全鏈，冪等與回驗只能實跑
- **scope**：佇列格式、產生權、套用腳本、回驗、對帳出列；不含 patch 落地（T-22）、不含面板顯示（T-23）
- **不可逆動作**：**無**（套用腳本只寫 issue metadata，皆可人工回復）
- **測試先行**：先寫冪等與產生權兩條斷言取得紅燈
> **排序理由（回應依賴考量）**：本票**排在 T-08 之前**、只依 T-07。理由：手動階段所有 GitHub 寫入都得走佇列，T-08 的「落 `sc:station-1`」本身就是一次寫入——若佇列未就緒，T-08 在手動階段根本無法驗收。只依 T-07（需要 label scheme 才知道 payload 形狀），不依 T-06，故可與 plugin 骨架平行推進。

## T-22 · 程式碼 patch 落地路徑
**Blocked by**：T-21
**What it delivers**：站 4 的程式產出以 **patch 檔**形式進佇列，由同一支本機腳本套用到目標 repo 工作區；未套用前該產出不得被視為已交付。
- **REQ-ID**：REQ-§4.6（程式碼落地路徑）
- **驗收條件（可執行）**：① 造一份 patch 型佇列項 ⇒ 套用後目標工作區檔案內容與 patch 預期相符；② 同一 patch 套用兩次 ⇒ 第二次因現況已符而跳過，不產生重複套用或衝突；③ **未套用前的交付判定**：patch 仍在佇列時查該票狀態 ⇒ 必須顯示為「未交付」，不得因 executor 回報完成就算數；④ 套用衝突（目標已被改動）⇒ 具名回報並留在佇列，不得部分套用後宣稱成功。**紅燈設計**：先寫③斷言 ⇒ 必紅（未實作時 executor 自陳即被當成交付）。**獨立預期值來源**＝patch 檔本身宣告的目標內容。
- **depends_on**：[T-21]
- **executor**：`fullstack-developer`｜**basis**：套用機制與 issue metadata 完全不同（寫工作區、可能衝突），與 T-21 分開才裝得進單一 context
- **scope**：patch 型佇列項與其套用、衝突處理、交付判定；不含站 4 驗收邏輯本身（T-14）
- **不可逆動作**：**無**（套用至工作區，未 commit 前可還原；🚫 腳本不得自動 commit 或 push）
- **測試先行**：先寫「未套用不得算交付」斷言取得紅燈

## ~~T-23~~ · 已併入 T-09（依 quiz 裁示）
面板揭露「待寫 N 筆」與「寫入斷點期間不可信」標記，**不另立票**——T-09 尚未開工，字卡欄位一次做齊較省。交付與驗收內容見末節「既有票增修」的 T-09 列。票號 T-23 保留空號，不重排後續票。

## T-24 · session 內 loop 主體（選件三判準＋六條停止條件）
**Blocked by**：T-10、T-12、T-13（增修後）
**What it delivers**：`/station-run` 的 loop 模式——持續「選件 → 派工 → 收件 → 判 gate → 續派」，依三條判準構成 frontier，遇六條停止條件之一即停手並具名停因。
- **REQ-ID**：REQ-§5.3a
- **驗收條件（可執行）**：① **選件三判準**：造四張票（有未解 blocker／`depends_on` 未完成／已有在跑 executor／三條皆滿足）⇒ 只有第四張進 frontier；② **停因⑤**：清空 frontier ⇒ loop 退出且具名「停因：frontier 空」；③ **停因①**：下一張票的第八欄宣告「不可逆：有」⇒ dispatch **前**停手並具名；④ **停因②**：同一票 rework 2 次仍 fail ⇒ 停手、不再自動重派；⑤ **停因③**：工作位於站 1／2／3 ⇒ 不自動推進，停在人類 gate；⑥ 站 4／5 內部的 dispatch／驗收／雙審／merge 全程**未出現任何確認提問**；⑦ **停因⑥**：把外廠累計成本推過 100% ⇒ loop 停手、具名停因⑥、已產出結果照常輸出。**紅燈設計**：先寫①（三判準過濾）與③（不可逆前停手）兩條斷言 ⇒ 必紅（未實作時全選、且不看第八欄）。**獨立預期值來源**＝四張人造票的欄位配置與第八欄宣告值。
- **depends_on**：[T-10, T-12, T-13]
- **executor**：`fullstack-developer`｜**basis**：loop 疊在既有派工與 gate 之上，停手時機只能靠實跑多輪驗證
- **scope**：loop 主體、frontier 判準、六條停止條件；不含通知與痕跡（T-25）、不含跨 session 排程（deferred-to-CI）
- **不可逆動作**：**無**（loop 本身只編排；停止條件①正是為了攔住票級不可逆動作）。⚠️ 具名承認：**dispatch 後 executor 內部自行觸發的不可逆動作不在本票涵蓋範圍**（§5.3a 已具名）
- **測試先行**：先寫三判準過濾與不可逆前停手兩條斷言取得紅燈

## T-25 · 停因通知與收尾批次摘要
**Blocked by**：T-24
**What it delivers**：停因②③④主動推播通知使用者；停因①⑤安靜退出；loop 收尾留一行批次摘要（本輪派了哪些票、gate 判定結果），續跑期間不逐輪留痕。
- **REQ-ID**：REQ-§5.3a（痕跡與通知）
- **驗收條件（可執行）**：① 分別觸發五種停因 ⇒ ②③④三種產生推播、①⑤兩種安靜；② loop 跑滿三輪後停止 ⇒ 產生**恰一則**批次摘要（非三則），內容涵蓋三輪的派票與判定；③ 摘要與停因紀錄以留言型佇列項產生（手動階段），套用後可在對應票或 anchor 上讀到；④ 摘要不參與任何 gate 判定（移除摘要後重跑 gate ⇒ 結果不變）。**紅燈設計**：先寫①（②③④必通知）與②（恰一則）兩條斷言 ⇒ 必紅（未實作時全部安靜、或逐輪洗版）。**獨立預期值來源**＝五種停因的人造觸發條件與輪數計數。
- **depends_on**：[T-24]
- **executor**：`fullstack-developer`｜**basis**：通知與痕跡是可獨立觀察的行為，且錯誤模式（該叫不叫／不該叫狂叫）需獨立驗收
- **scope**：停因通知、批次摘要；不含 loop 選件與停止判定本身
- **不可逆動作**：**無**（對外推播僅限使用者本人通知，非對外寄送）
- **測試先行**：先寫「②③④必通知」與「恰一則摘要」兩條斷言取得紅燈

## T-26 · CI 階段 spec（收攏全部 deferred-to-CI 條目）
**Blocked by**：T-15b
**What it delivers**：一份 CI 階段 spec，把 v1.8 中全部標記 deferred-to-CI 的條目收攏成單一清單並補齊其啟用規格：§3.4 timeline 歸因全套、§5.3b 跨 session 排程、§7.5 排程頻率與 prompt 自足、驗收 #7／#17／#25、風險 2 與 9 的緩解轉換、§9a 的第二階段內容；並明定觸發條件與 `github-actions[bot]` 身分接線。**本票存在的理由**：v1.8 有 9 處 deferred-to-CI 散落各章無人認領，而 ADR-NP-010 的成因正是「需求無人重新安置」——這張票就是那個安置點。
- **REQ-ID**：REQ-§9a、REQ-§5.3b、REQ-§7.5、REQ-§3.4
- **驗收條件（可執行）**：① 對 v1.8 全文抓出所有 `deferred-to-CI` 標記，逐一在新 spec 中找到對應章節 ⇒ **覆蓋率 100%，零遺漏**；② 每個條目附「啟用條件」與「啟用後如何驗收」兩欄；③ 新 spec 經站 2 出口 gate（第三方缺口審）。**紅燈設計**：撰寫前先跑 deferred 標記覆蓋率檢查 ⇒ 必紅（覆蓋率 0）。**獨立預期值來源**＝v1.8 全文中 `deferred-to-CI` 字串的出現位置清單（凍結為 fixture）。
- **depends_on**：[T-15b]
- **executor**：`planner`｜**basis**：產出是 spec，依路由表站 2 由 planner 承接；本票不寫任何實作
- **scope**：CI 階段 spec 撰寫；不含任何 CI workflow 實作
- **不可逆動作**：**無**
- **測試先行**：先凍結 deferred 標記 fixture 並跑覆蓋率檢查取得紅燈
> **為何開這張票**：**已裁示開為實票，非佔位票**。v1.8 有 **9 處**條文被標 deferred-to-CI 且散落各章，無人認領。ADR-NP-010 的成因記錄得很清楚——「站 1 漏採 high autonomy、舊心跳隨狀態檔一併作廢，**無人重新安置該需求**」。一組沒有負責人的延後條款正是同型病的溫床。blocked by T-15b 使它**不會進 frontier 被 loop 誤派**（§9a：走完第一條完整生產線後才升 CI），同時它在票集裡有名字、有 REQ-ID、有覆蓋率驗收，不會再度蒸發。

---

## 依賴序（新票）

```
T-07 → T-21 ─┬─ T-22
             ├─ T-09（併入原 T-23 的待寫揭露）
             └─（為 T-08 起所有寫入型票的前提，見增修清單）

T-10 ┐
T-12 ┼→ T-24 → T-25
T-13 ┘

T-15b → T-26
```

**可最早開工的新票**：T-21（僅依 T-07，可與 T-06／T-08 平行準備）

---

## 既有票必要增修（不重寫原檔，逐條具名）

| 票 | 增修內容 | 為何不違反 🔒 |
|---|---|---|
| **T-13** | 票模板與站 3 gate 檢查由**七欄改八欄**，新增「不可逆動作：有／無＋說明」；驗收由四張票擴為五張（增一張缺第八欄者）。**這是增修，不開新票** | T-13 **無 🔒 標記**，本就允許增修；且第八欄是同一份欄位檢查的延伸，另開票會讓兩張票同時碰同一份檢查邏輯（違反 DRY） |
| **T-09** | **併入原 T-23**：字卡新增「待寫 N 筆」，且佇列非空時「在跑數量」與「停滯票數」加註「寫入斷點期間不可信」；佇列檔不存在時顯示「待寫佇列不存在」而非 0；board 白名單擴充為可讀佇列檔。blocked by **加 T-21** | T-09 無 🔒 且尚未開工；同一支 board、同一份字卡渲染，分兩票會讓兩人同時改同一段渲染邏輯 |
| **T-08**〔🔒〕 | ① blocked by **加 T-21**；② 手動階段驗收改以「佇列中存在對應寫入項且 payload 正確」認定；③ **刪除**「以使用者 OAuth 重跑 ⇒ 拒絕初始化」該條——已被 ADR-NP-009 推翻，改為「報告含固定字串『手動階段：無機器歸因，依 ADR-NP-009』」 | 🔒 禁的是 **scope 增肥（加新功能）**；本次三項皆為**驗收認定方式**隨平台限制與上游 ADR 變更而調整，未增加任何交付物。③ 更是**移除**一條已失效的驗收 |
| **T-10**〔🔒〕 | ① blocked by 加 T-21；② 手動階段驗收改以佇列項認定；③ 歸因與復位相關驗收標 **deferred-to-CI**（§3.4 手動階段不生效） | 同上：驗收方式調整與上游 ADR 連動，未加功能。③ 是**縮減**測試範圍，與增肥反向 |
| **T-12、T-14、T-15a、T-15b、T-18** | blocked by 各加 T-21；手動階段驗收一律改以「佇列項存在且 payload 正確」認定 | 五張皆無 🔒；且此為**單一橫向修正**（手動階段唯一寫入路徑），逐票重寫反而製造五份不一致的說法 |

🔴 **本次 🔒 票增修經 Commander 核可**（T-08、T-10）：核可的是「驗收認定方式隨平台限制與上游 ADR 調整、未增交付物、其中兩項為縮減」這一具體理由。**🔒 不得增肥條款未被推翻，仍然生效**——日後任何為 T-08／T-10 新增交付物的請求，一律開新票。

> **統一修正條款（已採納，取代逐票複述）**：
> 「**手動階段所有 GitHub 寫入型驗收，一律以『待寫佇列中存在對應動作項且 payload 正確』認定；實際落地與回驗由 T-21 的套用腳本負責。升 CI 後本條款自動失效，回歸直接寫入驗收。**」

---

## quiz 裁示紀錄

1. **T-21 不拆**——理由採納：產生端與套用端共用同一份 payload 契約，分兩人做最容易在邊界對不上；且佇列已簡化為四欄，體積可控。
2. **T-23 併回 T-09**——T-09 尚未開工，字卡欄位一次做齊；票號不重排。
3. **T-24／T-25 不併**——停因通知是防「靈體卡住」（loop 停了但沒人知道）的唯一防線，值得自己的紅燈。
4. **T-26 開為實票**（非佔位票）——v1.8 有 9 處 deferred-to-CI 散落無人認領，而 ADR-NP-010 的成因正是「需求無人重新安置」。
5. **既有票增修照統一修正條款辦**，不逐票複述；🔒 票增修經核可但條款續行。
