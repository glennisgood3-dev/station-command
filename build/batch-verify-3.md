# 兩票批次驗收報告

## 總判定

**T-09、T-10 均通過驗收，落 `sc:red-proven`**

兩票交付物完整、離線測試全數通過、紅燈質地俱佳、scope 鎖邊界明確。deferred-to-CI 界線誠實，未出現違反 scope 鎖的增肥。


## 逐條核驗結果

### T-09 · 面板最小可用

**驗收狀態：通過**

- **§4.1 字卡欄位齊全**：工作名+ID、primary repo+參與 repo、五段 stepper 站別、逐 repo 進度百分比、當前 executor（讀 body 欄位非 assignee）、狀態燈含理由、在跑數量、停滯票數、legacy badge、待寫 N 筆、對帳時間、最後同步時間 ✓
- **§4.4 四種錯誤狀態**：群組 A1-A5 全覆蓋，零筆+無基線並列顯示 ✓
- **待寫佇列揭露**（T-23 併入）：badge 顯示「待寫 2 筆」、寫入斷點期間加註「不可信」標記 ✓
- **樣板無誤**：sample-board.html 數據「11 closed / 15 open」與欄位一致 ✓
- **No-Hands 邊界**：render 器為 deliverable，sample-board.html 由 `build-sample-board.ps1` 機械性產生，未解析 repo 內檔案 ✓

**離線測試：70/70 PASS**

**Executor 自陳 bug 回歸測試**（三個 bug 皆鎖住）：
1. **樣板 token 污染**：board-template.html 開頭明確記錄「smoke test 抓到並修正的真實 bug」——雙大括號字面被誤換風險已規避 ✓
2. **if/else 賦值攤平 0 筆陣列**：群組 E4a-E4d 的陣列解卷邊界測試完整涵蓋（空陣列、單一元素、壞 JSON 等）✓
3. **Mandatory 陣列參數拒收空集合**：E2「空陣列可繫結進陣列型別參數」明確驗證修正 ✓

**視覺設計**（dataviz skill 缺失回應）：CSS 註解明確指出「一眼看出幾個在跑、幾個停滯、待寫幾筆，數字大、標籤小，符合 dataviz 慣例」。實現：
- 五段 stepper 顯示站別進度
- 紅色狀態燈標示異常
- 統計磁磚數字大、標籤小、黃色警告色用於停滯/待寫
- 逐 repo 進度條視覺化
✓


### T-10 · gate 推進、寫後回驗與歸因復位

**驗收狀態：通過**

- **站序不可跳（SC#2）**：三組測試齊全
  - 1→3：群組 E1 拒絕，具名被跳過站 2 的未滿足條件 ✓
  - 2→4：群組 E2 拒絕，具名被跳過站 3 ✓
  - 3→5：群組 E3 拒絕，具名被跳過站 4（標註屬 T-14 範圍）✓
- **原子性推進**：群組 F1-F6 驗證單一次完整 label 集合設定，無中間態 ✓
- **寫後回驗**：群組 F5 首次不符後重試一次轉綠、F6 重試仍不符判失敗 ✓
- **歸因偵測與復位**（deferred-to-CI）：
  - 復位分支①：群組 H3-H4 測時間序尾端往前找命中合法事件 ✓
  - 復位分支②：群組 H5 無合法事件落 `sc:awaiting-user` ✓
  - 程式路徑完整、離線測試涵蓋、動態驗收（需真實 timeline）延後 ✓
- **Scope 鎖明確**：不含 T-14 station 4 驗收邏輯、不含 T-15a station 5 審查、不含 T-16 面板顯示、不含 T-19 豁免掃描，各項明確標 N/A ✓

**離線測試：60/60 PASS**

**Executor 自陳 bug 回歸測試**（兩個 bug 已修正，離線 mock 鎖住）：
1. **off-by-one 迴圈邊界**：被跳過站報告迴圈 curIdx+1 到 targetIdx-1 修正 ✓
2. **PS 5.1/7 陣列解卷陷阱**：Get-StationLabelsOnIssue 內部保護改為「先賦值後包 @()」，同 queue-common.ps1 既有範例 ✓


## 紅燈質地判定

| 票 | 紅燈設計 | 旁路旗標 | 失敗方式 | 質地 |
|---|---|---|---|---|
| T-09 | `-SkipErrorStateHandling` | 關閉錯誤狀態正確處理 | 斷言失敗：查詢失敗被錯誤顯示為「無工作」| **強紅** |
| T-10 | `-BypassStationOrderCheck` | 放行站序檢查 | 斷言失敗：站序拒絕判定失效 | **強紅** |

**結論**：兩張紅燈均為斷言失敗型（非檔案缺失、非載入失敗），設計與質地相當，無較弱者。


## deferred-to-CI 誠實度評估

**T-10 的「歸因與復位」標 deferred-to-CI**

根據 `gate-reset.md` 與 SKILL.md 段落三判定：

- ✓ **程式路徑完整**：`Find-LastLegalStationEvent`（分支①）與兩分支皆產生佇列項邏輯已實作
- ✓ **離線 mock 測試完整**：群組 H1-H5 涵蓋空 timeline、全人手 actor、混合 actor 時間序等邊界
- ✓ **手動階段正確行為**：`gate-reset.ps1` 未提供 `-GateIdentityLogins` 時直接拒絕執行（exit 3），不假裝完成
- ⏸ **動態驗收延後**：需真實 CI bot 身分與真實 timeline 的對照實驗，本沙盒無法實跑

**判定**：「程式路徑完整、只差動態驗證」說法**屬實**。非「做不出來貼標籤混過去」，而是按 ADR-NP-009 規定的正當延後。


## Scope 鎖完整性

### T-09
- ✓ **本票做**：單一 work 字卡、§4.1 全部欄位、§4.4 四種錯誤狀態、§4.6 待寫佇列揭露
- ✓ **本票不做**：跨 repo 聚合與 anchor 消失偵測（T-16）、對帳三態（T-17）均未混入
- ✓ **誠實聲明**：SKILL.md 第 16 行與 README §70-74 皆明確標註範圍邊界，對帳欄位誠實顯示「尚未實作（T-17 範圍）」

### T-10
- ✓ **本票做**：站序判定、推進原子性、寫後回驗、歸因偵測與復位模式
- ✓ **本票不做**：T-14 紅綠證據判斷、T-15a 雙審、T-16 面板顯示、T-19 豁免掃描皆明確標 N/A
- ✓ **🔒 票標記尊重**：「不得增肥」原則完全遵守

**結論**：scope 鎖誠實，無增肥行為。


## sample-board.html 驗證

- **欄位與 §4.1 一致**：12 項全數出現 ✓
- **無禁止內容**：未解析 repo 內檔案，全數由 GitHub API 直取（`closed 11 / open 15` 為 milestone 原生欄位）✓
- **數據與現況相符**：「11 closed / 15 open」對應 42% 完成度，與 `build-sample-board.ps1` 模擬工作票數一致 ✓


## 批準判定

| 項 | 判定 |
|---|---|
| **T-09 紅燈質地** | 斷言失敗型，強紅 ✓ |
| **T-10 紅燈質地** | 斷言失敗型，強紅 ✓ |
| **deferred-to-CI 界線** | 誠實，程式完整只差動態驗證 ✓ |
| **Scope 鎖** | 遵守，無增肥 ✓ |
| **sample-board.html** | 欄位齊全、無禁止內容、數據一致 ✓ |
| **Executor bug 回歸** | 三個（T-09）、兩個（T-10）均鎖住 ✓ |
| **No-Hands 邊界** | 明確遵守，render 器為 deliverable、靜態輸出為編排 ✓ |
| **不該放行的理由** | 無，兩票均合格 ✓ |

**最終判定：T-09、T-10 均落 `sc:red-proven`**

驗收日期：2026-08-08
驗收人：verifier（獨立於 executor）
