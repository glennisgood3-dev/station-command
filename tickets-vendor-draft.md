# station-command · 站 3 補票：異廠 executor 路由（未發布）

依據：Spec **v1.11（定稿）**§5.1 廠商登錄表（含 Gemini 免費層計費事實更正）／§5.2 站 5 雙審（軸內 worst issue 與兩廠界線）／§5.4 異廠呼叫規格／§5.5 分歧裁決／§7.6 成本邊界（含「上限」＝金額或配額兩種語意）
規則：Pocock to-tickets ＋ 本專案**八欄位**（含第八欄「不可逆動作」）
範圍：**只補異廠相關新票，編號自 T-27 起**；既有票增修列於末節。
狀態：**已過使用者裁示，本檔為定稿。**

---

## T-27 · 廠商登錄表 asset（12 家）
**Blocked by**：無（T-11 已關票）
**What it delivers**：隨 plugin 出貨的廠商登錄簿 asset——12 家的 provider 名／端點／認證方式／格式家族／狀態；並將既有路由表 asset 的 executor 欄語意擴為「provider ＋ model」（內建 Claude sub-agent 可省略 provider）。完成即可 demo：讀 asset 就知道有哪些 provider 可用、缺什麼才能啟用。
- **REQ-ID**：REQ-§5.1
- **驗收條件（可執行）**：① asset 列數為 12，逐列的端點字串與實測清單**逐字相符**：`api.openai.com/v1`｜`generativelanguage.googleapis.com/v1beta`｜`openrouter.ai/api/v1`｜`api.x.ai/v1`｜`api.deepseek.com`｜`api.mistral.ai/v1`｜`api.groq.com/openai/v1`｜`api.together.xyz/v1`｜`api.perplexity.ai`｜`api.cohere.com/v1`｜`api.fireworks.ai/inference/v1`｜`api.cerebras.ai/v1`；② 格式家族僅取四值之一（OpenAI-compatible／Gemini／Anthropic／Cohere）；③ 狀態僅取 `available`／`registered-no-key`，且 Gemini 為 `available`、其餘 11 家為 `registered-no-key`；④ asset 內含「登錄≠啟用」與「外廠按量計費」兩句具名警語。**紅燈設計**：先寫「12 列且端點逐字相符」比對 ⇒ 必紅（asset 不存在）。**獨立預期值來源**＝2026-08-08 實測端點清單（凍結為 fixture，外部既存事實）。
- **depends_on**：[]
- **executor**：`docs-manager`｜**basis**：屬正典表格落地，需與實測清單逐字對齊，判斷成分低、對齊成分高
- **scope**：登錄表 asset 與路由表 executor 欄語意擴充；不含任何呼叫實作
- **不可逆動作**：**無**
- **測試先行**：先凍結端點 fixture 並跑逐字比對取得紅燈

## T-28 · 異廠呼叫適配層、key 路徑、逾時重試與降級
**Blocked by**：T-27
**What it delivers**：四個格式家族各一份**最小適配**（依 provider＋model 組出請求、解出回應）、key 自連接資料夾讀取、逾時與至多一次重試、失敗即**具名降級**。完成即可 demo：指定任一 provider 發一次呼叫，成功則回內容，失敗則產生具名降級記錄且生產線不中斷。
- **REQ-ID**：REQ-§5.4
- **驗收條件（可執行）**：① **請求構造**：對四個家族各組一次請求，逐一比對其結構與該家族的文件化 schema（欄位名、認證 header 位置）；② **可達性**：對 12 家各發一次最小請求 ⇒ 回應碼皆落在「連得到」區間（200／401／403／429），無網路層攔截；③ **key 不外洩**：全程檢查對話輸出、repo 內容、log 與待寫佇列 ⇒ 皆不含 key 字串；④ **逾時重試**：模擬逾時與 5xx ⇒ 各重試恰一次後判該 provider 不可用，**不得無限重試**；⑤ **降級具名**：任一失敗 ⇒ 產生含 provider／失敗原因／承接者三項的降級記錄，且生產線繼續。**紅燈設計**：先寫③（key 不得出現在四處任一處）與④（重試次數恰為 1）兩條斷言 ⇒ 必紅。**獨立預期值來源**＝四家族的官方請求 schema 與實測回應碼 fixture。
- **depends_on**：[T-27]
- **executor**：`fullstack-developer`｜**basis**：穿過「讀 key → 構造請求 → 送出 → 解析或降級」全鏈，重試與洩漏只能實跑驗證
- **scope（涵蓋範圍明寫，🚫 不得含糊）**：本票**只驗到「請求形狀正確且送得到」為止**——即請求結構符合各家族 schema、12 家皆可達、key 不外洩、重試恰一次、失敗降級具名。**本票不驗、也不宣稱**「對方正確理解了請求並回出可用內容」，該端到端證明**由 T-31 承擔**。比照 T-21 的靜態／動態分層先例（格式契約先立、真實落地後驗）。不含成本記帳（T-29）、不含站 5 接線（T-30）
- **不可逆動作**：**無**（唯讀呼叫外部 API；🚫 適配層不得帶任何寫入型或付費升級型呼叫）。⚠️ 具名：呼叫本身會產生**費用**，費用不可退但非「不可逆動作」定義所指的 deploy／刪資料類，邊界由 §7.6 管
- **測試先行**：先寫 key 不外洩與重試次數兩條斷言取得紅燈

## T-29 · 成本邊界：每票上限、逐次記帳、三段告警
**Blocked by**：T-28、T-25、T-24
**What it delivers**：每票 token 上限與超限停手、每次呼叫的 token 與估算成本寫入該票 gate 紀錄、累計達 50%／80% 告警續跑、100% 強停且已產出結果照常輸出。
- **REQ-ID**：REQ-§7.6
- **驗收條件（可執行）**：① 把每票上限調到極低 ⇒ 呼叫在超限點**停手並具名回報**，且 🚫 輸入與輸出**皆未被截斷**（比對送出內容與回傳內容的完整性）；② 連續呼叫三次 ⇒ gate 紀錄出現三筆 token 與成本記帳，數值與各次回應回報的用量相符；③ 人為將累計推過 50%／80% ⇒ 各發一次告警且流程續跑；④ 推過 100% ⇒ **依 §5.3a 停止條件⑥停手**（🚫 本票不自行實作停手規則，只負責觸發），loop 具名停因⑥，**且該票已產出的審查結果照常輸出**（停的是後續呼叫，不是既有成果）；⑤ 告警走 T-25 既有的推播管道，🚫 不另立第二套通知機制。**紅燈設計**：先寫①（超限須停手且不得靜默截斷）與④（強停但既有結果仍輸出）兩條斷言 ⇒ 必紅。**獨立預期值來源**＝人為設定的上限值與各次 API 回應中的 usage 欄位。
- **depends_on**：[T-28, T-25, T-24]
- **executor**：`fullstack-developer`｜**basis**：記帳與門檻判定貼著呼叫路徑，與 T-28 同層但錯誤模式（靜默截斷）需獨立驗收
- **scope**：token 上限、記帳、三段告警、**觸發** §5.3a 停因⑥；不含停手規則本身（屬 §5.3a／T-24）、不含通知管道實作（沿用 T-25）
- **不可逆動作**：**無**
- **測試先行**：先寫「超限不得靜默截斷」斷言取得紅燈
> ✅ **原阻擋已解除**：spec §5.3a 停止條件由五條改為**六條**（新增⑥外廠累計成本達 100%，理由具名「花下去拿不回來，與①不可逆同級」），§7.6 改為指向 §5.3a 不自立停手規則——見 Spec v1.10。本票的 blocked by 因此**加 T-24**（停因⑥的實作點）。

## T-30 · 站 5 A 軸異廠接線與分歧裁決
**Blocked by**：T-15a、T-28
**What it delivers**：站 5 A 軸（規範審）可路由至外廠 provider；兩廠 finding 各自完整留存、不合併不跨廠重排；重疊處標示為最強訊號；分歧處由 gate 具名列出交使用者裁示。
- **REQ-ID**：REQ-§5.2、REQ-§5.5
- **驗收條件（可執行）**：① 一票經 DECISIONS.md 裁示升兩廠 ⇒ A 軸實際由外廠 provider 執行，B 軸維持 Claude；② A 軸輸入僅含 diff ＋ 規範清單 ＋ 12 條 smell 基線（核對送出內容，**不得含 spec 原文**）；③ 兩份 finding 清單各自完整、順序未跨廠重排（比照 SC#6 的輸入分離驗法，核對兩份原始輸出）；④ 人造一條兩廠皆提出的 finding ⇒ 標示為重疊；⑤ 人造一條僅單廠提出的 finding ⇒ gate 具名列出為分歧並要求裁示，**輸出中不得出現多數決或「以 Claude 為準」字樣**；⑥ 外廠不可用 ⇒ 依 T-28 降級為單廠雙軸並具名，gate 不阻塞；⑦ **升兩廠後 A 軸的兩份廠別清單，各自報出一個軸內 worst issue**（兩個，各屬其廠），且輸出中**不存在跨廠合選的單一 worst**——依 Spec v1.11:234（跨廠合選即 §5.5 禁止的跨廠重排）。**紅燈設計**：先寫③（不得合併重排）與⑤（禁多數決字樣）兩條斷言 ⇒ 必紅。**獨立預期值來源**＝人造的兩份 finding 清單原文與其重疊／分歧配置。
- **depends_on**：[T-15a, T-28]
- **executor**：`fullstack-developer`｜**basis**：需在既有雙審編排上掛外廠路由並改動 finding 呈現，兩者共用同一份報告結構
- **scope**：A 軸 provider 路由、finding 並列與分歧標示（**含兩廠各自的軸內 worst issue，不跨廠合選**）；不含 B 軸外包、不含站 2／站 3 的異廠接線（另行開票）
- **不可逆動作**：**無**
- **測試先行**：先寫「不得合併重排」與「禁多數決字樣」兩條斷言取得紅燈
> **為何開新票而非 T-15a 增修**：T-15a 是全票集體積最大者、**已因體積拆過一次**（原 T-15 拆成 15a／15b）；再塞入 provider 路由與分歧裁決會重蹈覆轍。且異廠接線需要 T-15a 完成後才有掛載點，先後關係天然成立。

## T-31 · Gemini 首發端到端實測
**Blocked by**：T-28
**What it delivers**：以真實 key 完成一次端到端外廠審查呼叫（送出 → 取得內容 → **解析成 finding 清單** → 記帳），並證明**回應體結構不符時不會被誤讀成「沒有 finding」**；結論經 PR 寫入 plugin repo DECISIONS.md。
- **REQ-ID**：REQ-§5.1（首發）、REQ-§5.4
- **驗收條件（可執行）**：① **錯誤體不得被誤讀為空清單**：對 `generativelanguage.googleapis.com/v1beta` 送一次指定**免費層不存在的 model 名**的請求 ⇒ 回應為錯誤 JSON（`error.code`／`error.status` 結構、非 `candidates` 結構）⇒ 適配層須 **(a)** 判定解析失敗、**(b)** 產生含 provider／失敗原因／承接者三項的具名降級記錄、**(c)** **不**寫入該次 usage 記帳；② **正常路徑**：以真實 key 送一次含實際 diff 的規範審請求 ⇒ 回 200 且內容被解析為 **finding 清單**（逐條可列舉，非整包字串）；③ 該次呼叫的 token 與成本落入 gate 紀錄（銜接 T-29）；④ **登錄表中 Gemini 狀態字串為 `available（首發；免費層，無需計費）`**（對齊 Spec v1.11:184），🚫 全檔不得殘留「待儲值」字串；⑤ 結論條目落檔，含實際回應碼與用量原文。**紅燈設計（斷言失敗型，符 Spec v1.11 §6 註 A）**：先跑①的錯誤體情境並寫下 (a)(b)(c) 三條斷言 ⇒ **必紅**——未實作端到端解析前，適配層會把錯誤 JSON 當成「沒有 finding」而靜默放行（＝假綠），(a)(b) 至少一條失敗。⚠️ 具名：本紅燈是**斷言失敗的紅**（請求送得到、回應收得到、程式跑到斷言才失敗），🚫 **不是載入／collection 失敗**，也 🚫 不再依賴「配額 0 回 403／429」。**獨立預期值來源（對齊 Spec v1.11 §3.5 註 C）**＝Gemini API 官方文件的 **v1beta `generateContent` 成功回應 schema（`candidates[].content`）與錯誤回應 schema（`error.code`／`error.status`）**——**外部既存事實，非由被測適配層自身產生**。
- **depends_on**：[T-28]
- **executor**：`researcher`｜**basis**：本質是外部服務真實行為的事實查核（比照 T-02），成功與錯誤兩種回應體皆須向真實端點取得，非實作題
- **scope**：單次端到端實測（成功路徑＋錯誤體路徑）與結論落檔；不含把 Gemini 接進任何站別（T-30）。⚠️ **與 T-28 的界線**：T-28 只驗到「請求形狀正確且送得到」，其降級驗收針對**傳輸層失敗**（逾時／5xx／key 無效）；本票驗的是**回應體語意層**——對方回了、但回的不是可用內容 ⇒ T-28 scope 欄已明寫此段由 T-31 承擔，兩票不重疊
- **不可逆動作**：**無**——免費層唯讀推論呼叫，不產生費用（Spec v1.11:199：官方 tier 表 Free 層計費欄為 N/A）、不寫入對方系統、不寄送、不開權限；**所消耗者為 RPM／RPD 配額，而配額隨時間窗自行回復**（Spec v1.11:315 已具名「配額觸頂不等於資源不可回收」）⇒ 不符 §3.5 第八欄「不可逆」定義，**不觸發 §5.3a 停止條件①**，得由 loop 自動 dispatch。⚠️ **兩項殘留具名**：① 配額是**跨票共享資源**，本票燒掉的 RPD 會排擠同日其他票的外廠呼叫——屬**外部性非不可逆**，由 §7.6 每票上限與三段告警承接，🚫 不另立停手規則（§5.3a 為窮舉正典）；② **重判觸發條件**：若日後該 key 改綁付費層，本欄須重判為「**有 ⇒ 產生實際費用**」並回復人類 gate
- **測試先行**：**seam**＝適配層對 Gemini 回應體的**解析出口**（輸入＝原始回應 JSON，輸出＝finding 清單或降級記錄），🚫 不打在 HTTP client 內部。先寫①的 (a)(b)(c) 三條斷言取得紅燈，再跑②求綠
> **外部前提已解除（事實更正，2026-08-08）**：原票假設「Gemini 首發需使用者儲值」，並以「儲值前必得 403／429」當紅燈。**兩項皆為事實錯誤**：Gemini 免費層不需開啟計費，真跑會直接回 200 ⇒ 該紅燈紅不起來。故本票**移除外部前提、正常進 frontier**（仍 blocked by T-28），紅燈改為斷言失敗型（見上）。

---

## 依賴序（新票）

```
T-27 → T-28 ─┬─ T-29 ←── T-25、T-24
             ├─ T-30 ←── T-15a
             └─ T-31
```
**可立即開工**：T-27（無 blocker）

---

## 既有票必要增修

| 票 | 增修內容 | 說明 |
|---|---|---|
| **T-11**（已關票） | 路由表 asset 的 executor 欄語意擴為「provider ＋ model」，內建 Claude sub-agent 可省略 provider | **不重開 T-11**，此修改由 **T-27 一併執行**（同一份 asset、同一個 executor，分兩票會讓兩人改同一檔） |
| **T-13** | 票模板 `executor` 欄的值格式擴為「provider＋model」；站 3 gate 檢查同步接受該格式 | T-13 無 🔒，可增修；且第八欄增修已在前批列入，本次併同一輪處理較省 |
| **T-24** | 五條停止條件改六條，新增停因⑥（外廠累計成本 100%）之實作與驗收 | T-24 尚未開工（blocked by 依賴未完成），票面直接改，不另開票 |

其餘既有票**不動**，依賴圖未受影響（T-11、T-21 皆已關票，不構成新阻擋）。

---

## 已裁決（原阻擋事項）

**§7.6 與 §5.3a 的規則對撞已解除，採方案 (a)**：ADR-NP-010② 增訂第 6 條停止條件「外廠累計成本達上限 100%」，理由具名「**花下去拿不回來，與第①條不可逆同級**」；50%／80% 僅告警不停手。並加註「**本清單為窮舉，新增停止條件一律改本條並具名，不得散落他處**」。Spec 已同步改為 **v1.10**（§5.3a 五條→六條、§7.6 改為指向 §5.3a）。

## 待辦（未開票，具名理由）

🔴 **本節存在的目的是防止重蹈 ADR-NP-010 的成因**——「需求無人重新安置」。以下兩項已在 spec 有條文（§5.4 適用站別），但**現在不開票**，理由具名如下；日後掛載點就緒時，由本節認領人開票。

| 待辦項 | 為何現在不開票 | 開票觸發條件 |
|---|---|---|
| **站 2 出口缺口審的異廠接線** | 站 2 gate 的實作尚未排定票號，現在開票會變成長期不進 frontier 的殭屍票 | 站 2 出口 gate 的實作票排定後 |
| **站 3 拆票複核的異廠接線** | 掛載點在 T-13，而 T-13 本身尚有第八欄與 provider 欄兩項增修待完成 | T-13 增修完成並關票後 |

## 裁示紀錄（原顆粒度疑慮）

1. **T-30 不拆**（已裁示）——原疑慮：**T-30 可能偏大**——A 軸 provider 路由與分歧裁決是兩件事（一件是「打到哪」、一件是「回來怎麼呈現」）。可拆 T-30a 路由／T-30b 分歧裁決。我沒先拆，是因為分歧裁決若沒有真的兩份異廠 finding 就無從驗收，拆開會讓 T-30b 得自造假資料驗收，反而弱化紅燈。**已裁示不拆**（理由採納：拆開會讓分歧裁決得自造假的兩廠報告驗收，紅燈強度直接弱掉；合起來用真實兩廠輸出驗收才有意義）。
2. **T-28 不與 T-31 合併**（已裁示）——「請求形狀對」本身即可獨立驗收的切片（比照 T-21 靜態／動態分層先例），合併會讓整張票被儲值卡住。T-28 的 scope 欄已明寫涵蓋範圍到哪為止、端到端由 T-31 承擔。⚠️ **附註（2026-08-08 事實更正）**：本條原理由之一「合併會讓整張票被儲值卡住」**已失效**（Gemini 免費層不需儲值）；但**裁示結論不變**——不合併的有效理由改為分層仍然成立：T-28 驗傳輸層（形狀對、送得到），T-31 驗語意層（回的是不是可用內容），兩者錯誤模式不同、紅燈各自獨立。
3. **站 2／站 3 異廠接線現在不開票**（已裁示）——理由與開票觸發條件已具名於上方「待辦（未開票）」節，避免需求再度無人安置。
